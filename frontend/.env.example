# Browser requests use this base URL. Keep /api for same-origin cookies in development.
VUE_APP_API_BASE_URL=/api

# Vue dev-server forwards /api to the Go service.
VUE_APP_DEV_API_TARGET=http://localhost:8081
