# 云网盘系统

一个基于 Vue 3 + Element Plus 的现代化云网盘前端系统。

## 功能特性

### 核心功能
- 📁 文件管理：上传、下载、删除、重命名文件
- 📂 文件夹管理：创建、删除、重命名文件夹
- 🔍 文件搜索：支持文件名和内容搜索
- ⭐ 文件收藏：收藏重要文件，快速访问
- 📤 文件共享：生成分享链接，设置访问权限
- 🗂️ 文件分类：按类型、标签、时间分类管理
- 🗑️ 回收站：安全删除，支持恢复

### 个人中心
- 👤 用户信息管理：头像、用户名、邮箱、手机号
- 📊 存储统计：使用量统计、文件数量统计
- 📈 文件类型分析：各类文件占比可视化
- 🔐 安全设置：密码修改、账户安全
- 📱 响应式设计：支持移动端访问

### 系统设置
- 🎨 界面设置：主题切换、显示方式、语言设置
- 🔒 隐私设置：文件可见性、访问权限控制
- 🔔 通知设置：邮件通知、应用内通知
- 🔄 同步设置：自动同步、冲突处理
- ⚙️ 高级设置：缓存管理、性能优化

## 技术栈

- **前端框架**: Vue 3 (Composition API)
- **UI 组件库**: Element Plus
- **路由管理**: Vue Router 4
- **状态管理**: Vuex 4
- **HTTP 客户端**: Axios
- **构建工具**: Vue CLI

## 项目结构

```
src/
├── api/           # API 接口
│   ├── auth.js    # 认证相关
│   ├── file.js    # 文件相关
│   └── user.js    # 用户相关
├── components/    # 组件
│   ├── layout/    # 布局组件
│   └── ...        # 其他组件
├── views/         # 页面
│   ├── Login.vue  # 登录页
│   ├── UserProfile.vue  # 个人中心
│   ├── Settings.vue     # 设置页面
│   └── ...        # 其他页面
├── router/        # 路由配置
├── store/         # 状态管理
└── utils/         # 工具函数
```

## 快速开始

### 安装依赖
```bash
npm install
```

### 启动开发服务器
```bash
npm run serve
```

### 构建生产版本
```bash
npm run build
```

## 主要页面

### 个人中心 (`/user`)
- **基本信息管理**: 头像上传、用户信息编辑
- **存储统计**: 实时显示存储使用情况
- **文件统计**: 文件数量、类型分布
- **安全设置**: 密码修改功能

### 系统设置 (`/settings`)
- **界面设置**: 主题、显示方式、语言
- **隐私设置**: 文件可见性、访问权限
- **通知设置**: 邮件和应用内通知
- **同步设置**: 自动同步配置
- **高级设置**: 缓存、性能优化

## API 接口

### 用户相关
- `GET /me` - 获取用户信息
- `PUT /me` - 更新用户信息
- `POST /me/avatar` - 上传头像
- `GET /me/stats` - 获取用户统计
- `PUT /me/password` - 修改密码
- `GET /me/file-stats` - 获取文件类型统计

### 文件相关
- `POST /file/list` - 获取文件列表
- `POST /upload` - 上传文件
- `GET /files/{id}/download` - 下载文件
- `DELETE /files/{id}` - 删除文件
- `POST /files/{id}/rename` - 重命名文件

## 开发说明

### 模拟数据
当前版本使用模拟数据，便于前端开发和测试。在生产环境中，请：
1. 取消注释真实的 API 调用代码
2. 注释掉模拟数据代码
3. 确保后端 API 接口正确实现

### 样式变量
系统使用 CSS 变量定义主题色彩，可在 `src/assets/global.css` 中修改：
- `--primary-color`: 主色调
- `--bg-color`: 背景色
- `--card-bg`: 卡片背景色
- `--text-primary`: 主要文字颜色
- `--text-secondary`: 次要文字颜色

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License
